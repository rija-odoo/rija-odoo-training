<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">tree,form</field>
        <field name="domain"> [('property_type_id', '=', active_id)]</field>
    </record>
    <record id="estate_property_view_offer_tree" model="ir.ui.view">
        <field name="name">estate.property.view.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree editable='bottom' decoration-success="status == 'accepted'"
                decoration-danger="status == 'refused'">
                <field name="partner_id" string="Partner"></field>
                <field name="price" string="Price"></field>
                <field name="validity" string="Validity(Days)"></field>
                <field name="date_deadline" string="Deadline"></field>
                <button name="action_accepted" type="object" icon="fa-check"
                    attrs="{'invisible':[('status','in',['accepted','refused'])]}" title="accept" />
                <button name="action_refused" type="object" icon="fa-close"
                    attrs="{'invisible':[('status','in',['accepted','refused'])]}" title="refuse" />
                <field name="status" string="Status"></field>
            </tree>
        </field>
    </record>
</odoo>